SECRET_KEY=change-me
ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=montash
DB_USER=montash
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
# Узнать: добавить бота в группу менеджеров, отправить там /id
TELEGRAM_STAFF_GROUP_CHAT_ID=

PRIVATE_STORAGE_ROOT=

# Сгенерировать: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Обязательно задать в проде — без него сканы паспортов шифруются ключом, выведенным из SECRET_KEY.
PASSPORT_ENCRYPTION_KEY=
